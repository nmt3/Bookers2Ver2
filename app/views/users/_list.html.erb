<h2>User info</h2>
  <table class="table">
    <tbody>
      <tr>
        <%= image_tag user.get_profile_image %>
      </tr>
      <tr>
        <th>name</th>
        <th><%= user.name %></th>
      </tr>
      <tr>
        <th>introduction</th>
        <th><%= user.introduction %></th>
      </tr>
    </tbody>
  </table>
  <div class="row">
    <%#= link_to "★", edit_user_path(current_user), class: "btn btn-outline-secondary btn-block" %>
      <%= link_to "★", edit_user_path(user), class: "btn btn-outline-secondary btn-block" %>
  </div>
  <h2>New Book</h2>
      <%#仮コード%>
    <%= form_with model: book do |f| %>
    <h4>タイトル</h4>
      <%= f.text_field :title %>

    <h4>本文</h4>
      <%= f.text_area :body %>

    <%= f.submit '投稿' %>
    <% end %>
