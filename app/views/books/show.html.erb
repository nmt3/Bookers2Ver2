<p id="notice"></p>
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/list', user: @user, book: @book %>
    </div>

    <div class="col-md-8 offset-md-1">
        <h2>Book datail</h2>
          <table class="table">
            <tbody>
              <tr>
                <th>
                  <%= image_tag @user.get_profile_image, size: "100×100" %>
                </th>
                <th>
                  <%= link_to @books.title, book_path(@books) %>
                </th>
                <th>
                  <%= @books.body %>
                </th>
                <th>
                  <%= link_to "Edit", edit_book_path(@books.id), class: "btn btn-sm btn-success" %>
                </th>
                <th>
                  <% if @books.user == current_user %>
                    <%= link_to "Destroy", book_path(@books.id), method: :delete, class: "btn btn-sm btn-danger" %>
                  <% end %>
                </th>
              </tr>
            </tbody>
          </table>
      </div>
  </div>
</div>